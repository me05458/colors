int basicLines(int s); //kind of ugly, draws varying color in rows, just kind of happens. only takes size s.
int forfunsies(int h, int w); //draws stars, kind of badly, h and w are size of window.
int radialMode(int h, int w); //gradients up and down, works nicely, currently h doesn't matter, w is size.
void logg(int i, int j); //debugging function to log to file
void testScreen(int h, int w); //displays pretty square to see how big screen must be.
